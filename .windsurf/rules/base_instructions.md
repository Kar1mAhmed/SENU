---
trigger: always_on
---

- Don't run the server
- Always make sure you use types from lib/types.ts
- Make sure to user reusable components
- buttons and reusable components should be all in one place under the component folder.